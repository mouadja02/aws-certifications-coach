# Streamlit Cloud Secrets Configuration
# Add these secrets in Streamlit Cloud dashboard under Settings > Secrets
# OR create a local .streamlit/secrets.toml file (DO NOT COMMIT)

# Snowflake Configuration (Native Streamlit Integration)
[connections.snowflake]
account = "abc12345.us-east-1"  # Your Snowflake account identifier
user = "your_username"
password = "your_password"
role = "ACCOUNTADMIN"
warehouse = "COMPUTE_WH"
database = "AWS_CERTIFICATIONS"
schema = "PUBLIC"
client_session_keep_alive = true

