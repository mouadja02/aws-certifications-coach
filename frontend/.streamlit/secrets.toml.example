# Streamlit Cloud Secrets Configuration
# Add these secrets in Streamlit Cloud dashboard under Settings > Secrets

# Backend API Configuration
BACKEND_URL = "https://your-api-domain.elasticbeanstalk.com"

# Database Configuration (optional - for direct connection if needed)
DB_HOST = "your-rds-endpoint.rds.amazonaws.com"
DB_PORT = "5432"
DB_NAME = "aws_certifications_prod"
DB_USER = "awscoach_prod"
DB_PASSWORD = "your-strong-password"
